apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: tm
  region: us-east-1

iam:
  withOIDC: true

nodeGroups:
  - name: ng-1
    instanceType: t3.micro
    desiredCapacity: 2
    minSize: 1
    maxSize: 2