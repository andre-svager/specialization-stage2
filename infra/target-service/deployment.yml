apiVersion: apps/v1
kind: Deployment
metadata:
  name: target-service
  namespace: togglemaster
spec:
  replicas: 1
  selector:
    matchLabels:
      app: target-service
  template:
    metadata:
      labels:
        app: target-service
    spec:
      containers:
        - name: target-service
          image: 973397181776.dkr.ecr.us-east-1.amazonaws.com/target-service:latest
          ports:
            - containerPort: 8003
          readinessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: togglemaster-config
                  key: TARGET_PORT
          envFrom:
            - configMapRef:
                name: togglemaster-config
            - secretRef:
                name: target-secret